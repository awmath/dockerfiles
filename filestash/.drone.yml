kind: pipeline
name: filestash

steps:
  - name: docker
    image: plugins/docker
    settings:
      repo: awmath/filestash
      context: ./filestash
      username:
        from_secret: docker_username
      password:
        from_secret: docker_password
    when:
      branch:
      - master

trigger:
  cron: [ weekly ]
